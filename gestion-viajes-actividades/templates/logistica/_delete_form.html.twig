<form method="post" action="{{ path('app_logistica_delete', {'id': logistica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logistica.id) }}">
    <button class="btn">Delete</button>
</form>
